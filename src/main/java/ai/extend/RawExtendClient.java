/**
 * Raw-response Extend client - not auto-generated by Fern.
 *
 * <p>Fern generates {@link RawExtendClientBase}; this hand-written subclass preserves the
 * historical {@code RawExtendClient} type name (returned by {@link ExtendClient#withRawResponse()}).</p>
 */
package ai.extend;

import ai.extend.core.ClientOptions;

/**
 * Provides access to raw HTTP responses (status code, headers) alongside the deserialized body.
 */
public class RawExtendClient extends RawExtendClientBase {

    public RawExtendClient(ClientOptions clientOptions) {
        super(clientOptions);
    }
}
