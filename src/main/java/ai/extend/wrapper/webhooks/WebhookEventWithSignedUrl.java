/**
 * Custom wrapper code - not auto-generated by Fern.
 */
package ai.extend.wrapper.webhooks;

/**
 * Represents a webhook event with a signed URL payload.
 *
 * <p>Use {@link Webhooks#fetchSignedPayload} to retrieve the full payload
 * from the signed URL.</p>
 */
public class WebhookEventWithSignedUrl {

    private final String eventId;
    private final String eventType;
    private final SignedDataUrlPayload payload;

    public WebhookEventWithSignedUrl(String eventId, String eventType, SignedDataUrlPayload payload) {
        this.eventId = eventId;
        this.eventType = eventType;
        this.payload = payload;
    }

    /**
     * Returns the unique identifier for this webhook event.
     */
    public String getEventId() {
        return eventId;
    }

    /**
     * Returns the type of event (e.g., "workflow_run.completed").
     */
    public String getEventType() {
        return eventType;
    }

    /**
     * Returns the signed URL payload.
     */
    public SignedDataUrlPayload getPayload() {
        return payload;
    }

    @Override
    public String toString() {
        return "WebhookEventWithSignedUrl{"
                + "eventId='" + eventId + '\''
                + ", eventType='" + eventType + '\''
                + ", payload=" + payload
                + '}';
    }
}
