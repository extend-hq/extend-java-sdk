/**
 * Asynchronous Extend API client - not auto-generated by Fern.
 *
 * <p>Fern generates {@link AsyncExtendClientBase}; this hand-written subclass preserves the
 * historical {@code AsyncExtendClient} type name and builder return types.</p>
 */
package ai.extend;

import ai.extend.core.ClientOptions;

/**
 * Asynchronous (CompletableFuture-based) Extend API client.
 */
public class AsyncExtendClient extends AsyncExtendClientBase {

    public AsyncExtendClient(ClientOptions clientOptions) {
        super(clientOptions);
    }

    /**
     * Returns the raw-response client. Preserves the historical {@link AsyncRawExtendClient}
     * return type so existing {@code AsyncRawExtendClient raw = client.withRawResponse();} keeps compiling.
     */
    @Override
    public AsyncRawExtendClient withRawResponse() {
        return new AsyncRawExtendClient(this.clientOptions);
    }

    /**
     * Creates a new builder for {@link AsyncExtendClient}.
     */
    public static AsyncExtendClientBuilder builder() {
        return new AsyncExtendClientBuilder();
    }
}
