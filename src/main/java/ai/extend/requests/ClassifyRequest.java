/**
 * This file was auto-generated by Fern from our API Definition.
 */
package ai.extend.requests;

import ai.extend.core.ObjectMappers;
import ai.extend.types.ClassifyConfigBase;
import ai.extend.types.ClassifyRequestClassifier;
import ai.extend.types.ClassifyRequestFile;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = ClassifyRequest.Builder.class)
public final class ClassifyRequest {
    private final Optional<ClassifyRequestClassifier> classifier;

    private final Optional<ClassifyConfigBase> config;

    private final ClassifyRequestFile file;

    private final Optional<Map<String, Object>> metadata;

    private final Map<String, Object> additionalProperties;

    private ClassifyRequest(
            Optional<ClassifyRequestClassifier> classifier,
            Optional<ClassifyConfigBase> config,
            ClassifyRequestFile file,
            Optional<Map<String, Object>> metadata,
            Map<String, Object> additionalProperties) {
        this.classifier = classifier;
        this.config = config;
        this.file = file;
        this.metadata = metadata;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Reference to an existing classifier. One of <code>classifier</code> or <code>config</code> must be provided.
     */
    @JsonProperty("classifier")
    public Optional<ClassifyRequestClassifier> getClassifier() {
        return classifier;
    }

    /**
     * @return Inline classify configuration. One of <code>classifier</code> or <code>config</code> must be provided.
     */
    @JsonProperty("config")
    public Optional<ClassifyConfigBase> getConfig() {
        return config;
    }

    /**
     * @return The file to be classified. Files can be provided as a URL, an Extend file ID, or raw text.
     */
    @JsonProperty("file")
    public ClassifyRequestFile getFile() {
        return file;
    }

    @JsonProperty("metadata")
    public Optional<Map<String, Object>> getMetadata() {
        return metadata;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof ClassifyRequest && equalTo((ClassifyRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(ClassifyRequest other) {
        return classifier.equals(other.classifier)
                && config.equals(other.config)
                && file.equals(other.file)
                && metadata.equals(other.metadata);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.classifier, this.config, this.file, this.metadata);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static FileStage builder() {
        return new Builder();
    }

    public interface FileStage {
        /**
         * <p>The file to be classified. Files can be provided as a URL, an Extend file ID, or raw text.</p>
         */
        _FinalStage file(@NotNull ClassifyRequestFile file);

        Builder from(ClassifyRequest other);
    }

    public interface _FinalStage {
        ClassifyRequest build();

        /**
         * <p>Reference to an existing classifier. One of <code>classifier</code> or <code>config</code> must be provided.</p>
         */
        _FinalStage classifier(Optional<ClassifyRequestClassifier> classifier);

        _FinalStage classifier(ClassifyRequestClassifier classifier);

        /**
         * <p>Inline classify configuration. One of <code>classifier</code> or <code>config</code> must be provided.</p>
         */
        _FinalStage config(Optional<ClassifyConfigBase> config);

        _FinalStage config(ClassifyConfigBase config);

        _FinalStage metadata(Optional<Map<String, Object>> metadata);

        _FinalStage metadata(Map<String, Object> metadata);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements FileStage, _FinalStage {
        private ClassifyRequestFile file;

        private Optional<Map<String, Object>> metadata = Optional.empty();

        private Optional<ClassifyConfigBase> config = Optional.empty();

        private Optional<ClassifyRequestClassifier> classifier = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(ClassifyRequest other) {
            classifier(other.getClassifier());
            config(other.getConfig());
            file(other.getFile());
            metadata(other.getMetadata());
            return this;
        }

        /**
         * <p>The file to be classified. Files can be provided as a URL, an Extend file ID, or raw text.</p>
         * <p>The file to be classified. Files can be provided as a URL, an Extend file ID, or raw text.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("file")
        public _FinalStage file(@NotNull ClassifyRequestFile file) {
            this.file = Objects.requireNonNull(file, "file must not be null");
            return this;
        }

        @java.lang.Override
        public _FinalStage metadata(Map<String, Object> metadata) {
            this.metadata = Optional.ofNullable(metadata);
            return this;
        }

        @java.lang.Override
        @JsonSetter(value = "metadata", nulls = Nulls.SKIP)
        public _FinalStage metadata(Optional<Map<String, Object>> metadata) {
            this.metadata = metadata;
            return this;
        }

        /**
         * <p>Inline classify configuration. One of <code>classifier</code> or <code>config</code> must be provided.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage config(ClassifyConfigBase config) {
            this.config = Optional.ofNullable(config);
            return this;
        }

        /**
         * <p>Inline classify configuration. One of <code>classifier</code> or <code>config</code> must be provided.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "config", nulls = Nulls.SKIP)
        public _FinalStage config(Optional<ClassifyConfigBase> config) {
            this.config = config;
            return this;
        }

        /**
         * <p>Reference to an existing classifier. One of <code>classifier</code> or <code>config</code> must be provided.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage classifier(ClassifyRequestClassifier classifier) {
            this.classifier = Optional.ofNullable(classifier);
            return this;
        }

        /**
         * <p>Reference to an existing classifier. One of <code>classifier</code> or <code>config</code> must be provided.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "classifier", nulls = Nulls.SKIP)
        public _FinalStage classifier(Optional<ClassifyRequestClassifier> classifier) {
            this.classifier = classifier;
            return this;
        }

        @java.lang.Override
        public ClassifyRequest build() {
            return new ClassifyRequest(classifier, config, file, metadata, additionalProperties);
        }
    }
}
