/**
 * This file was auto-generated by Fern from our API Definition.
 */
package ai.extend.resources.workflowversions;

import ai.extend.core.ClientOptions;
import ai.extend.core.RequestOptions;
import ai.extend.resources.workflowversions.requests.WorkflowVersionsCreateRequest;
import ai.extend.resources.workflowversions.requests.WorkflowVersionsListRequest;
import ai.extend.resources.workflowversions.types.WorkflowVersionsListResponse;
import ai.extend.types.WorkflowVersion;

public class WorkflowVersionsClient {
    protected final ClientOptions clientOptions;

    private final RawWorkflowVersionsClient rawClient;

    public WorkflowVersionsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawWorkflowVersionsClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawWorkflowVersionsClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * List all versions of a workflow, including the draft version. Returns a paginated list of version summaries.
     */
    public WorkflowVersionsListResponse list(String id) {
        return this.rawClient.list(id).body();
    }

    /**
     * List all versions of a workflow, including the draft version. Returns a paginated list of version summaries.
     */
    public WorkflowVersionsListResponse list(String id, RequestOptions requestOptions) {
        return this.rawClient.list(id, requestOptions).body();
    }

    /**
     * List all versions of a workflow, including the draft version. Returns a paginated list of version summaries.
     */
    public WorkflowVersionsListResponse list(String id, WorkflowVersionsListRequest request) {
        return this.rawClient.list(id, request).body();
    }

    /**
     * List all versions of a workflow, including the draft version. Returns a paginated list of version summaries.
     */
    public WorkflowVersionsListResponse list(
            String id, WorkflowVersionsListRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(id, request, requestOptions).body();
    }

    /**
     * Deploy a new version of a workflow. The deployed version becomes available for running workflow runs.
     * <p>When <code>steps</code> is omitted, the current draft is deployed as-is. When <code>steps</code> is provided, the given steps are deployed directly without modifying the draft.</p>
     */
    public WorkflowVersion create(String id) {
        return this.rawClient.create(id).body();
    }

    /**
     * Deploy a new version of a workflow. The deployed version becomes available for running workflow runs.
     * <p>When <code>steps</code> is omitted, the current draft is deployed as-is. When <code>steps</code> is provided, the given steps are deployed directly without modifying the draft.</p>
     */
    public WorkflowVersion create(String id, RequestOptions requestOptions) {
        return this.rawClient.create(id, requestOptions).body();
    }

    /**
     * Deploy a new version of a workflow. The deployed version becomes available for running workflow runs.
     * <p>When <code>steps</code> is omitted, the current draft is deployed as-is. When <code>steps</code> is provided, the given steps are deployed directly without modifying the draft.</p>
     */
    public WorkflowVersion create(String id, WorkflowVersionsCreateRequest request) {
        return this.rawClient.create(id, request).body();
    }

    /**
     * Deploy a new version of a workflow. The deployed version becomes available for running workflow runs.
     * <p>When <code>steps</code> is omitted, the current draft is deployed as-is. When <code>steps</code> is provided, the given steps are deployed directly without modifying the draft.</p>
     */
    public WorkflowVersion create(String id, WorkflowVersionsCreateRequest request, RequestOptions requestOptions) {
        return this.rawClient.create(id, request, requestOptions).body();
    }

    /**
     * Get a specific version of a workflow, including its step definitions.
     */
    public WorkflowVersion retrieve(String id, String versionId) {
        return this.rawClient.retrieve(id, versionId).body();
    }

    /**
     * Get a specific version of a workflow, including its step definitions.
     */
    public WorkflowVersion retrieve(String id, String versionId, RequestOptions requestOptions) {
        return this.rawClient.retrieve(id, versionId, requestOptions).body();
    }
}
