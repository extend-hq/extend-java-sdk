/**
 * This file was auto-generated by Fern from our API Definition.
 */
package ai.extend.resources.workflowversions;

import ai.extend.core.ClientOptions;
import ai.extend.core.RequestOptions;
import ai.extend.resources.workflowversions.requests.WorkflowVersionsCreateRequest;
import ai.extend.resources.workflowversions.requests.WorkflowVersionsListRequest;
import ai.extend.resources.workflowversions.types.WorkflowVersionsListResponse;
import ai.extend.types.WorkflowVersion;
import java.util.concurrent.CompletableFuture;

public class AsyncWorkflowVersionsClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawWorkflowVersionsClient rawClient;

    public AsyncWorkflowVersionsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawWorkflowVersionsClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawWorkflowVersionsClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * List all versions of a workflow, including the draft version. Returns a paginated list of version summaries.
     */
    public CompletableFuture<WorkflowVersionsListResponse> list(String id) {
        return this.rawClient.list(id).thenApply(response -> response.body());
    }

    /**
     * List all versions of a workflow, including the draft version. Returns a paginated list of version summaries.
     */
    public CompletableFuture<WorkflowVersionsListResponse> list(String id, RequestOptions requestOptions) {
        return this.rawClient.list(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * List all versions of a workflow, including the draft version. Returns a paginated list of version summaries.
     */
    public CompletableFuture<WorkflowVersionsListResponse> list(String id, WorkflowVersionsListRequest request) {
        return this.rawClient.list(id, request).thenApply(response -> response.body());
    }

    /**
     * List all versions of a workflow, including the draft version. Returns a paginated list of version summaries.
     */
    public CompletableFuture<WorkflowVersionsListResponse> list(
            String id, WorkflowVersionsListRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(id, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Deploy a new version of a workflow. The deployed version becomes available for running workflow runs.
     * <p>When <code>steps</code> is omitted, the current draft is deployed as-is. When <code>steps</code> is provided, the given steps are deployed directly without modifying the draft.</p>
     */
    public CompletableFuture<WorkflowVersion> create(String id) {
        return this.rawClient.create(id).thenApply(response -> response.body());
    }

    /**
     * Deploy a new version of a workflow. The deployed version becomes available for running workflow runs.
     * <p>When <code>steps</code> is omitted, the current draft is deployed as-is. When <code>steps</code> is provided, the given steps are deployed directly without modifying the draft.</p>
     */
    public CompletableFuture<WorkflowVersion> create(String id, RequestOptions requestOptions) {
        return this.rawClient.create(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Deploy a new version of a workflow. The deployed version becomes available for running workflow runs.
     * <p>When <code>steps</code> is omitted, the current draft is deployed as-is. When <code>steps</code> is provided, the given steps are deployed directly without modifying the draft.</p>
     */
    public CompletableFuture<WorkflowVersion> create(String id, WorkflowVersionsCreateRequest request) {
        return this.rawClient.create(id, request).thenApply(response -> response.body());
    }

    /**
     * Deploy a new version of a workflow. The deployed version becomes available for running workflow runs.
     * <p>When <code>steps</code> is omitted, the current draft is deployed as-is. When <code>steps</code> is provided, the given steps are deployed directly without modifying the draft.</p>
     */
    public CompletableFuture<WorkflowVersion> create(
            String id, WorkflowVersionsCreateRequest request, RequestOptions requestOptions) {
        return this.rawClient.create(id, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Get a specific version of a workflow, including its step definitions.
     */
    public CompletableFuture<WorkflowVersion> retrieve(String id, String versionId) {
        return this.rawClient.retrieve(id, versionId).thenApply(response -> response.body());
    }

    /**
     * Get a specific version of a workflow, including its step definitions.
     */
    public CompletableFuture<WorkflowVersion> retrieve(String id, String versionId, RequestOptions requestOptions) {
        return this.rawClient.retrieve(id, versionId, requestOptions).thenApply(response -> response.body());
    }
}
