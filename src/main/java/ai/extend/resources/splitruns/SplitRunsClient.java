/**
 * This file was auto-generated by Fern from our API Definition.
 */
package ai.extend.resources.splitruns;

import ai.extend.core.ClientOptions;
import ai.extend.core.RequestOptions;
import ai.extend.resources.splitruns.requests.SplitRunsCancelRequest;
import ai.extend.resources.splitruns.requests.SplitRunsCreateBatchRequest;
import ai.extend.resources.splitruns.requests.SplitRunsCreateRequest;
import ai.extend.resources.splitruns.requests.SplitRunsDeleteRequest;
import ai.extend.resources.splitruns.requests.SplitRunsListRequest;
import ai.extend.resources.splitruns.requests.SplitRunsRetrieveRequest;
import ai.extend.resources.splitruns.types.SplitRunsDeleteResponse;
import ai.extend.resources.splitruns.types.SplitRunsListResponse;
import ai.extend.types.BatchRun;
import ai.extend.types.SplitRun;

public class SplitRunsClient {
    protected final ClientOptions clientOptions;

    private final RawSplitRunsClient rawClient;

    public SplitRunsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawSplitRunsClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawSplitRunsClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * List all split runs.
     * <p>Returns a summary of each run. Use <code>GET /split_runs/{id}</code> to retrieve the full object including <code>output</code> and <code>config</code>.</p>
     */
    public SplitRunsListResponse list() {
        return this.rawClient.list().body();
    }

    /**
     * List all split runs.
     * <p>Returns a summary of each run. Use <code>GET /split_runs/{id}</code> to retrieve the full object including <code>output</code> and <code>config</code>.</p>
     */
    public SplitRunsListResponse list(RequestOptions requestOptions) {
        return this.rawClient.list(requestOptions).body();
    }

    /**
     * List all split runs.
     * <p>Returns a summary of each run. Use <code>GET /split_runs/{id}</code> to retrieve the full object including <code>output</code> and <code>config</code>.</p>
     */
    public SplitRunsListResponse list(SplitRunsListRequest request) {
        return this.rawClient.list(request).body();
    }

    /**
     * List all split runs.
     * <p>Returns a summary of each run. Use <code>GET /split_runs/{id}</code> to retrieve the full object including <code>output</code> and <code>config</code>.</p>
     */
    public SplitRunsListResponse list(SplitRunsListRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(request, requestOptions).body();
    }

    /**
     * Split a document into multiple parts using an existing splitter or an inline configuration.
     * <p>The request returns immediately with a <code>PROCESSING</code> status. Use webhooks or poll the Get Split Run endpoint for results.</p>
     */
    public SplitRun create(SplitRunsCreateRequest request) {
        return this.rawClient.create(request).body();
    }

    /**
     * Split a document into multiple parts using an existing splitter or an inline configuration.
     * <p>The request returns immediately with a <code>PROCESSING</code> status. Use webhooks or poll the Get Split Run endpoint for results.</p>
     */
    public SplitRun create(SplitRunsCreateRequest request, RequestOptions requestOptions) {
        return this.rawClient.create(request, requestOptions).body();
    }

    /**
     * Retrieve details about a specific split run, including its status and outputs.
     * <p>A common use case for this endpoint is to poll for the status and final output of a split run when using the <a href="https://docs.extend.ai/2026-02-09/api-reference/endpoints/split/create-split-run">Create Split Run</a> endpoint. For instance, if you do not want to not configure webhooks to receive the output via completion/failure events.</p>
     */
    public SplitRun retrieve(String id) {
        return this.rawClient.retrieve(id).body();
    }

    /**
     * Retrieve details about a specific split run, including its status and outputs.
     * <p>A common use case for this endpoint is to poll for the status and final output of a split run when using the <a href="https://docs.extend.ai/2026-02-09/api-reference/endpoints/split/create-split-run">Create Split Run</a> endpoint. For instance, if you do not want to not configure webhooks to receive the output via completion/failure events.</p>
     */
    public SplitRun retrieve(String id, RequestOptions requestOptions) {
        return this.rawClient.retrieve(id, requestOptions).body();
    }

    /**
     * Retrieve details about a specific split run, including its status and outputs.
     * <p>A common use case for this endpoint is to poll for the status and final output of a split run when using the <a href="https://docs.extend.ai/2026-02-09/api-reference/endpoints/split/create-split-run">Create Split Run</a> endpoint. For instance, if you do not want to not configure webhooks to receive the output via completion/failure events.</p>
     */
    public SplitRun retrieve(String id, SplitRunsRetrieveRequest request) {
        return this.rawClient.retrieve(id, request).body();
    }

    /**
     * Retrieve details about a specific split run, including its status and outputs.
     * <p>A common use case for this endpoint is to poll for the status and final output of a split run when using the <a href="https://docs.extend.ai/2026-02-09/api-reference/endpoints/split/create-split-run">Create Split Run</a> endpoint. For instance, if you do not want to not configure webhooks to receive the output via completion/failure events.</p>
     */
    public SplitRun retrieve(String id, SplitRunsRetrieveRequest request, RequestOptions requestOptions) {
        return this.rawClient.retrieve(id, request, requestOptions).body();
    }

    /**
     * Delete a split run and all associated data from Extend. This operation is permanent and cannot be undone.
     * <p>This endpoint can be used if you'd like to manage data retention on your own rather than automated data retention policies. Or make one-off deletions for your downstream customers.</p>
     */
    public SplitRunsDeleteResponse delete(String id) {
        return this.rawClient.delete(id).body();
    }

    /**
     * Delete a split run and all associated data from Extend. This operation is permanent and cannot be undone.
     * <p>This endpoint can be used if you'd like to manage data retention on your own rather than automated data retention policies. Or make one-off deletions for your downstream customers.</p>
     */
    public SplitRunsDeleteResponse delete(String id, RequestOptions requestOptions) {
        return this.rawClient.delete(id, requestOptions).body();
    }

    /**
     * Delete a split run and all associated data from Extend. This operation is permanent and cannot be undone.
     * <p>This endpoint can be used if you'd like to manage data retention on your own rather than automated data retention policies. Or make one-off deletions for your downstream customers.</p>
     */
    public SplitRunsDeleteResponse delete(String id, SplitRunsDeleteRequest request) {
        return this.rawClient.delete(id, request).body();
    }

    /**
     * Delete a split run and all associated data from Extend. This operation is permanent and cannot be undone.
     * <p>This endpoint can be used if you'd like to manage data retention on your own rather than automated data retention policies. Or make one-off deletions for your downstream customers.</p>
     */
    public SplitRunsDeleteResponse delete(String id, SplitRunsDeleteRequest request, RequestOptions requestOptions) {
        return this.rawClient.delete(id, request, requestOptions).body();
    }

    /**
     * Cancel an in-progress split run.
     * <p>Note: Only split runs with a status of <code>&quot;PROCESSING&quot;</code> can be cancelled. Splitter runs that have already completed, failed, or been cancelled cannot be cancelled again.</p>
     */
    public SplitRun cancel(String id) {
        return this.rawClient.cancel(id).body();
    }

    /**
     * Cancel an in-progress split run.
     * <p>Note: Only split runs with a status of <code>&quot;PROCESSING&quot;</code> can be cancelled. Splitter runs that have already completed, failed, or been cancelled cannot be cancelled again.</p>
     */
    public SplitRun cancel(String id, RequestOptions requestOptions) {
        return this.rawClient.cancel(id, requestOptions).body();
    }

    /**
     * Cancel an in-progress split run.
     * <p>Note: Only split runs with a status of <code>&quot;PROCESSING&quot;</code> can be cancelled. Splitter runs that have already completed, failed, or been cancelled cannot be cancelled again.</p>
     */
    public SplitRun cancel(String id, SplitRunsCancelRequest request) {
        return this.rawClient.cancel(id, request).body();
    }

    /**
     * Cancel an in-progress split run.
     * <p>Note: Only split runs with a status of <code>&quot;PROCESSING&quot;</code> can be cancelled. Splitter runs that have already completed, failed, or been cancelled cannot be cancelled again.</p>
     */
    public SplitRun cancel(String id, SplitRunsCancelRequest request, RequestOptions requestOptions) {
        return this.rawClient.cancel(id, request, requestOptions).body();
    }

    /**
     * Submit up to <strong>1,000 files</strong> for splitting in a single request. Each file is processed as an independent split run using the same splitter and configuration.
     * <p>Unlike the single <a href="https://docs.extend.ai/2026-02-09/api-reference/endpoints/split/create-split-run">Split File (Async)</a> endpoint, this batch endpoint accepts an <code>inputs</code> array and immediately returns a <code>BatchRun</code> object containing a batch <code>id</code> and a <code>PENDING</code> status. The individual runs are then queued and processed asynchronously.</p>
     * <p><strong>Monitoring results:</strong></p>
     * <ul>
     * <li><strong>Webhooks (recommended):</strong> Subscribe to <code>batch_processor_run.processed</code> and <code>batch_processor_run.failed</code> events. The webhook payload indicates the batch has finished — fetch individual run results using <code>GET /split_runs?batchId={id}</code>.</li>
     * <li><strong>Polling:</strong> Call <code>GET /batch_runs/{id}</code> to check the overall batch status, and use <code>GET /split_runs</code> filtered by <code>batchId</code> to retrieve individual run results.</li>
     * </ul>
     * <p><strong>Notes:</strong></p>
     * <ul>
     * <li>A processor reference (<code>splitter.id</code>) is required — inline <code>config</code> is not supported for batch requests.</li>
     * <li><code>inputs</code> must contain between 1 and 1,000 items.</li>
     * <li>All inputs in a batch use the same splitter version and override config.</li>
     * <li>Raw text input (<code>FileFromText</code>) is not supported for split runs. Use a URL or file ID.</li>
     * </ul>
     */
    public BatchRun createBatch(SplitRunsCreateBatchRequest request) {
        return this.rawClient.createBatch(request).body();
    }

    /**
     * Submit up to <strong>1,000 files</strong> for splitting in a single request. Each file is processed as an independent split run using the same splitter and configuration.
     * <p>Unlike the single <a href="https://docs.extend.ai/2026-02-09/api-reference/endpoints/split/create-split-run">Split File (Async)</a> endpoint, this batch endpoint accepts an <code>inputs</code> array and immediately returns a <code>BatchRun</code> object containing a batch <code>id</code> and a <code>PENDING</code> status. The individual runs are then queued and processed asynchronously.</p>
     * <p><strong>Monitoring results:</strong></p>
     * <ul>
     * <li><strong>Webhooks (recommended):</strong> Subscribe to <code>batch_processor_run.processed</code> and <code>batch_processor_run.failed</code> events. The webhook payload indicates the batch has finished — fetch individual run results using <code>GET /split_runs?batchId={id}</code>.</li>
     * <li><strong>Polling:</strong> Call <code>GET /batch_runs/{id}</code> to check the overall batch status, and use <code>GET /split_runs</code> filtered by <code>batchId</code> to retrieve individual run results.</li>
     * </ul>
     * <p><strong>Notes:</strong></p>
     * <ul>
     * <li>A processor reference (<code>splitter.id</code>) is required — inline <code>config</code> is not supported for batch requests.</li>
     * <li><code>inputs</code> must contain between 1 and 1,000 items.</li>
     * <li>All inputs in a batch use the same splitter version and override config.</li>
     * <li>Raw text input (<code>FileFromText</code>) is not supported for split runs. Use a URL or file ID.</li>
     * </ul>
     */
    public BatchRun createBatch(SplitRunsCreateBatchRequest request, RequestOptions requestOptions) {
        return this.rawClient.createBatch(request, requestOptions).body();
    }
}
