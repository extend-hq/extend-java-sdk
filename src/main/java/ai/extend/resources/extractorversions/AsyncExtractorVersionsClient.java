/**
 * This file was auto-generated by Fern from our API Definition.
 */
package ai.extend.resources.extractorversions;

import ai.extend.core.ClientOptions;
import ai.extend.core.RequestOptions;
import ai.extend.resources.extractorversions.requests.ExtractorVersionsCreateRequest;
import ai.extend.resources.extractorversions.requests.ExtractorVersionsListRequest;
import ai.extend.resources.extractorversions.types.ExtractorVersionsListResponse;
import ai.extend.types.ExtractorVersion;
import java.util.concurrent.CompletableFuture;

public class AsyncExtractorVersionsClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawExtractorVersionsClient rawClient;

    public AsyncExtractorVersionsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawExtractorVersionsClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawExtractorVersionsClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * This endpoint allows you to fetch all versions of a given extractor, including the current <code>draft</code> version.
     * <p>Versions are returned in descending order of creation (newest first) with the <code>draft</code> version first. The <code>draft</code> version is the latest unpublished version of the extractor, which can be published to create a new version. It might not have any changes from the last published version.</p>
     */
    public CompletableFuture<ExtractorVersionsListResponse> list(String extractorId) {
        return this.rawClient.list(extractorId).thenApply(response -> response.body());
    }

    /**
     * This endpoint allows you to fetch all versions of a given extractor, including the current <code>draft</code> version.
     * <p>Versions are returned in descending order of creation (newest first) with the <code>draft</code> version first. The <code>draft</code> version is the latest unpublished version of the extractor, which can be published to create a new version. It might not have any changes from the last published version.</p>
     */
    public CompletableFuture<ExtractorVersionsListResponse> list(String extractorId, RequestOptions requestOptions) {
        return this.rawClient.list(extractorId, requestOptions).thenApply(response -> response.body());
    }

    /**
     * This endpoint allows you to fetch all versions of a given extractor, including the current <code>draft</code> version.
     * <p>Versions are returned in descending order of creation (newest first) with the <code>draft</code> version first. The <code>draft</code> version is the latest unpublished version of the extractor, which can be published to create a new version. It might not have any changes from the last published version.</p>
     */
    public CompletableFuture<ExtractorVersionsListResponse> list(
            String extractorId, ExtractorVersionsListRequest request) {
        return this.rawClient.list(extractorId, request).thenApply(response -> response.body());
    }

    /**
     * This endpoint allows you to fetch all versions of a given extractor, including the current <code>draft</code> version.
     * <p>Versions are returned in descending order of creation (newest first) with the <code>draft</code> version first. The <code>draft</code> version is the latest unpublished version of the extractor, which can be published to create a new version. It might not have any changes from the last published version.</p>
     */
    public CompletableFuture<ExtractorVersionsListResponse> list(
            String extractorId, ExtractorVersionsListRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(extractorId, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * This endpoint allows you to publish a new version of an existing extractor. Publishing a new version creates a snapshot of the extractor's current configuration and makes it available for use in workflows.
     * <p>Publishing a new version does not automatically update existing workflows using this extractor. You may need to manually update workflows to use the new version if desired.</p>
     */
    public CompletableFuture<ExtractorVersion> create(String extractorId, ExtractorVersionsCreateRequest request) {
        return this.rawClient.create(extractorId, request).thenApply(response -> response.body());
    }

    /**
     * This endpoint allows you to publish a new version of an existing extractor. Publishing a new version creates a snapshot of the extractor's current configuration and makes it available for use in workflows.
     * <p>Publishing a new version does not automatically update existing workflows using this extractor. You may need to manually update workflows to use the new version if desired.</p>
     */
    public CompletableFuture<ExtractorVersion> create(
            String extractorId, ExtractorVersionsCreateRequest request, RequestOptions requestOptions) {
        return this.rawClient.create(extractorId, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Retrieve a specific version of an extractor in Extend
     */
    public CompletableFuture<ExtractorVersion> retrieve(String extractorId, String versionId) {
        return this.rawClient.retrieve(extractorId, versionId).thenApply(response -> response.body());
    }

    /**
     * Retrieve a specific version of an extractor in Extend
     */
    public CompletableFuture<ExtractorVersion> retrieve(
            String extractorId, String versionId, RequestOptions requestOptions) {
        return this.rawClient.retrieve(extractorId, versionId, requestOptions).thenApply(response -> response.body());
    }
}
