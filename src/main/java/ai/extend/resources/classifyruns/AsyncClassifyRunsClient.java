/**
 * This file was auto-generated by Fern from our API Definition.
 */
package ai.extend.resources.classifyruns;

import ai.extend.core.ClientOptions;
import ai.extend.core.RequestOptions;
import ai.extend.resources.classifyruns.requests.ClassifyRunsCreateRequest;
import ai.extend.resources.classifyruns.requests.ClassifyRunsListRequest;
import ai.extend.resources.classifyruns.types.ClassifyRunsDeleteResponse;
import ai.extend.resources.classifyruns.types.ClassifyRunsListResponse;
import ai.extend.types.ClassifyRun;
import java.util.concurrent.CompletableFuture;

public class AsyncClassifyRunsClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawClassifyRunsClient rawClient;

    public AsyncClassifyRunsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawClassifyRunsClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawClassifyRunsClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * List all classify runs.
     * <p>Returns a summary of each run. Use <code>GET /classify_runs/{id}</code> to retrieve the full object including <code>output</code> and <code>config</code>.</p>
     */
    public CompletableFuture<ClassifyRunsListResponse> list() {
        return this.rawClient.list().thenApply(response -> response.body());
    }

    /**
     * List all classify runs.
     * <p>Returns a summary of each run. Use <code>GET /classify_runs/{id}</code> to retrieve the full object including <code>output</code> and <code>config</code>.</p>
     */
    public CompletableFuture<ClassifyRunsListResponse> list(RequestOptions requestOptions) {
        return this.rawClient.list(requestOptions).thenApply(response -> response.body());
    }

    /**
     * List all classify runs.
     * <p>Returns a summary of each run. Use <code>GET /classify_runs/{id}</code> to retrieve the full object including <code>output</code> and <code>config</code>.</p>
     */
    public CompletableFuture<ClassifyRunsListResponse> list(ClassifyRunsListRequest request) {
        return this.rawClient.list(request).thenApply(response -> response.body());
    }

    /**
     * List all classify runs.
     * <p>Returns a summary of each run. Use <code>GET /classify_runs/{id}</code> to retrieve the full object including <code>output</code> and <code>config</code>.</p>
     */
    public CompletableFuture<ClassifyRunsListResponse> list(
            ClassifyRunsListRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Classify a document using an existing classifier or an inline configuration.
     * <p>The request returns immediately with a <code>PROCESSING</code> status. Use webhooks or poll the <a href="https://docs.extend.ai/2026-02-09/developers/api-reference/endpoints/classify/get-classify-run">Get Classify Run</a> endpoint for results.</p>
     */
    public CompletableFuture<ClassifyRun> create(ClassifyRunsCreateRequest request) {
        return this.rawClient.create(request).thenApply(response -> response.body());
    }

    /**
     * Classify a document using an existing classifier or an inline configuration.
     * <p>The request returns immediately with a <code>PROCESSING</code> status. Use webhooks or poll the <a href="https://docs.extend.ai/2026-02-09/developers/api-reference/endpoints/classify/get-classify-run">Get Classify Run</a> endpoint for results.</p>
     */
    public CompletableFuture<ClassifyRun> create(ClassifyRunsCreateRequest request, RequestOptions requestOptions) {
        return this.rawClient.create(request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Retrieve details about a specific classify run, including its status and outputs.
     * <p>A common use case for this endpoint is to poll for the status and final output of a classify run when using the <a href="https://docs.extend.ai/2026-02-09/developers/api-reference/endpoints/classify/create-classify-run">Create Classify Run</a> endpoint. For instance, if you do not want to not configure webhooks to receive the output via completion/failure events.</p>
     */
    public CompletableFuture<ClassifyRun> retrieve(String id) {
        return this.rawClient.retrieve(id).thenApply(response -> response.body());
    }

    /**
     * Retrieve details about a specific classify run, including its status and outputs.
     * <p>A common use case for this endpoint is to poll for the status and final output of a classify run when using the <a href="https://docs.extend.ai/2026-02-09/developers/api-reference/endpoints/classify/create-classify-run">Create Classify Run</a> endpoint. For instance, if you do not want to not configure webhooks to receive the output via completion/failure events.</p>
     */
    public CompletableFuture<ClassifyRun> retrieve(String id, RequestOptions requestOptions) {
        return this.rawClient.retrieve(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Delete a classify run and all associated data from Extend. This operation is permanent and cannot be undone.
     * <p>This endpoint can be used if you'd like to manage data retention on your own rather than automated data retention policies. Or make one-off deletions for your downstream customers.</p>
     */
    public CompletableFuture<ClassifyRunsDeleteResponse> delete(String id) {
        return this.rawClient.delete(id).thenApply(response -> response.body());
    }

    /**
     * Delete a classify run and all associated data from Extend. This operation is permanent and cannot be undone.
     * <p>This endpoint can be used if you'd like to manage data retention on your own rather than automated data retention policies. Or make one-off deletions for your downstream customers.</p>
     */
    public CompletableFuture<ClassifyRunsDeleteResponse> delete(String id, RequestOptions requestOptions) {
        return this.rawClient.delete(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Cancel an in-progress classify run.
     * <p>Note: Only classify runs with a status of <code>&quot;PROCESSING&quot;</code> can be cancelled. Classifier runs that have already completed, failed, or been cancelled cannot be cancelled again.</p>
     */
    public CompletableFuture<ClassifyRun> cancel(String id) {
        return this.rawClient.cancel(id).thenApply(response -> response.body());
    }

    /**
     * Cancel an in-progress classify run.
     * <p>Note: Only classify runs with a status of <code>&quot;PROCESSING&quot;</code> can be cancelled. Classifier runs that have already completed, failed, or been cancelled cannot be cancelled again.</p>
     */
    public CompletableFuture<ClassifyRun> cancel(String id, RequestOptions requestOptions) {
        return this.rawClient.cancel(id, requestOptions).thenApply(response -> response.body());
    }
}
