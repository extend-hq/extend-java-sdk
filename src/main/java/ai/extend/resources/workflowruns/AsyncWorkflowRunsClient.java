/**
 * This file was auto-generated by Fern from our API Definition.
 */
package ai.extend.resources.workflowruns;

import ai.extend.core.ClientOptions;
import ai.extend.core.RequestOptions;
import ai.extend.resources.workflowruns.requests.WorkflowRunsCreateBatchRequest;
import ai.extend.resources.workflowruns.requests.WorkflowRunsCreateRequest;
import ai.extend.resources.workflowruns.requests.WorkflowRunsListRequest;
import ai.extend.resources.workflowruns.requests.WorkflowRunsUpdateRequest;
import ai.extend.resources.workflowruns.types.WorkflowRunsCreateBatchResponse;
import ai.extend.resources.workflowruns.types.WorkflowRunsDeleteResponse;
import ai.extend.resources.workflowruns.types.WorkflowRunsListResponse;
import ai.extend.types.WorkflowRun;
import java.util.concurrent.CompletableFuture;

public class AsyncWorkflowRunsClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawWorkflowRunsClient rawClient;

    public AsyncWorkflowRunsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawWorkflowRunsClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawWorkflowRunsClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * List runs of a Workflow. Workflows are sequences of steps that process files and data in a specific order to achieve a desired outcome. A WorkflowRun represents a single execution of a workflow against a file.
     */
    public CompletableFuture<WorkflowRunsListResponse> list() {
        return this.rawClient.list().thenApply(response -> response.body());
    }

    /**
     * List runs of a Workflow. Workflows are sequences of steps that process files and data in a specific order to achieve a desired outcome. A WorkflowRun represents a single execution of a workflow against a file.
     */
    public CompletableFuture<WorkflowRunsListResponse> list(RequestOptions requestOptions) {
        return this.rawClient.list(requestOptions).thenApply(response -> response.body());
    }

    /**
     * List runs of a Workflow. Workflows are sequences of steps that process files and data in a specific order to achieve a desired outcome. A WorkflowRun represents a single execution of a workflow against a file.
     */
    public CompletableFuture<WorkflowRunsListResponse> list(WorkflowRunsListRequest request) {
        return this.rawClient.list(request).thenApply(response -> response.body());
    }

    /**
     * List runs of a Workflow. Workflows are sequences of steps that process files and data in a specific order to achieve a desired outcome. A WorkflowRun represents a single execution of a workflow against a file.
     */
    public CompletableFuture<WorkflowRunsListResponse> list(
            WorkflowRunsListRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Run a workflow with a file. A workflow is a sequence of steps that process files and data in a specific order to achieve a desired outcome.
     * <p>The request returns immediately with a <code>PROCESSING</code> status. Use webhooks or poll the Get Workflow Run endpoint for results.</p>
     */
    public CompletableFuture<WorkflowRun> create(WorkflowRunsCreateRequest request) {
        return this.rawClient.create(request).thenApply(response -> response.body());
    }

    /**
     * Run a workflow with a file. A workflow is a sequence of steps that process files and data in a specific order to achieve a desired outcome.
     * <p>The request returns immediately with a <code>PROCESSING</code> status. Use webhooks or poll the Get Workflow Run endpoint for results.</p>
     */
    public CompletableFuture<WorkflowRun> create(WorkflowRunsCreateRequest request, RequestOptions requestOptions) {
        return this.rawClient.create(request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Once a workflow has been run, you can check the status and output of a specific WorkflowRun.
     */
    public CompletableFuture<WorkflowRun> retrieve(String id) {
        return this.rawClient.retrieve(id).thenApply(response -> response.body());
    }

    /**
     * Once a workflow has been run, you can check the status and output of a specific WorkflowRun.
     */
    public CompletableFuture<WorkflowRun> retrieve(String id, RequestOptions requestOptions) {
        return this.rawClient.retrieve(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * You can update the name and metadata of an in progress WorkflowRun at any time using this endpoint.
     */
    public CompletableFuture<WorkflowRun> update(String id) {
        return this.rawClient.update(id).thenApply(response -> response.body());
    }

    /**
     * You can update the name and metadata of an in progress WorkflowRun at any time using this endpoint.
     */
    public CompletableFuture<WorkflowRun> update(String id, RequestOptions requestOptions) {
        return this.rawClient.update(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * You can update the name and metadata of an in progress WorkflowRun at any time using this endpoint.
     */
    public CompletableFuture<WorkflowRun> update(String id, WorkflowRunsUpdateRequest request) {
        return this.rawClient.update(id, request).thenApply(response -> response.body());
    }

    /**
     * You can update the name and metadata of an in progress WorkflowRun at any time using this endpoint.
     */
    public CompletableFuture<WorkflowRun> update(
            String id, WorkflowRunsUpdateRequest request, RequestOptions requestOptions) {
        return this.rawClient.update(id, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Delete a workflow run and all associated data from Extend. This operation is permanent and cannot be undone.
     * <p>This endpoint can be used if you'd like to manage data retention on your own rather than automated data retention policies. Or make one-off deletions for your downstream customers.</p>
     */
    public CompletableFuture<WorkflowRunsDeleteResponse> delete(String id) {
        return this.rawClient.delete(id).thenApply(response -> response.body());
    }

    /**
     * Delete a workflow run and all associated data from Extend. This operation is permanent and cannot be undone.
     * <p>This endpoint can be used if you'd like to manage data retention on your own rather than automated data retention policies. Or make one-off deletions for your downstream customers.</p>
     */
    public CompletableFuture<WorkflowRunsDeleteResponse> delete(String id, RequestOptions requestOptions) {
        return this.rawClient.delete(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Cancel a running workflow run by its ID. This endpoint allows you to stop a workflow run that is currently in progress.
     * <p>Note: Only workflow runs with a status of <code>PROCESSING</code> or <code>PENDING</code> can be cancelled. Workflow runs that are completed, failed, in review, rejected, or already cancelled cannot be cancelled.</p>
     */
    public CompletableFuture<WorkflowRun> cancel(String id) {
        return this.rawClient.cancel(id).thenApply(response -> response.body());
    }

    /**
     * Cancel a running workflow run by its ID. This endpoint allows you to stop a workflow run that is currently in progress.
     * <p>Note: Only workflow runs with a status of <code>PROCESSING</code> or <code>PENDING</code> can be cancelled. Workflow runs that are completed, failed, in review, rejected, or already cancelled cannot be cancelled.</p>
     */
    public CompletableFuture<WorkflowRun> cancel(String id, RequestOptions requestOptions) {
        return this.rawClient.cancel(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * This endpoint allows you to efficiently initiate large batches of workflow runs in a single request (up to 1,000 in a single request, but you can queue up multiple batches in rapid succession). It accepts an array of inputs, each containing a file and metadata pair. The primary use case for this endpoint is for doing large bulk runs of &gt;1000 files at a time that can process over the course of a few hours without needing to manage rate limits that would likely occur using the primary run endpoint.
     * <p>Unlike the single <a href="https://docs.extend.ai/2026-02-09/developers/api-reference/endpoints/workflow/run-workflow">Run Workflow</a> endpoint which returns the details of the created workflow runs immediately, this batch endpoint returns a <code>batchId</code>.</p>
     * <p>Our recommended usage pattern is to integrate with <a href="https://docs.extend.ai/2026-02-09/product/webhooks/configuration">Webhooks</a> for consuming results, using the <code>metadata</code> and <code>batchId</code> to match up results to the original inputs in your downstream systems. However, you can integrate in a polling mechanism by using a combination of the <a href="https://docs.extend.ai/2026-02-09/developers/api-reference/endpoints/workflow/list-workflow-runs">List Workflow Runs</a> endpoint to fetch all runs via a batch, and then <a href="https://docs.extend.ai/2026-02-09/developers/api-reference/endpoints/workflow/get-workflow-run">Get Workflow Run</a> to fetch the full outputs each run.</p>
     * <p><strong>Priority:</strong> All workflow runs created through this batch endpoint are automatically assigned a priority of 90.</p>
     * <p><strong>Processing and Monitoring:</strong>
     * Upon successful submission, the endpoint returns a <code>batchId</code>. The individual workflow runs are then queued for processing.</p>
     * <ul>
     * <li><strong>Monitoring:</strong> Track the progress and consume results of individual runs using <a href="https://docs.extend.ai/2026-02-09/product/webhooks/configuration">Webhooks</a>. Subscribe to events like <code>workflow_run.completed</code>, <code>workflow_run.failed</code>, etc. The webhook payload for these events will include the corresponding <code>batchId</code> and the <code>metadata</code> you provided for each input.</li>
     * <li><strong>Fetching Results:</strong> You can also use the <a href="https://docs.extend.ai/2026-02-09/developers/api-reference/endpoints/workflow/list-workflow-runs">List Workflow Runs</a> endpoint and filter using the <code>batchId</code> query param.</li>
     * </ul>
     */
    public CompletableFuture<WorkflowRunsCreateBatchResponse> createBatch(WorkflowRunsCreateBatchRequest request) {
        return this.rawClient.createBatch(request).thenApply(response -> response.body());
    }

    /**
     * This endpoint allows you to efficiently initiate large batches of workflow runs in a single request (up to 1,000 in a single request, but you can queue up multiple batches in rapid succession). It accepts an array of inputs, each containing a file and metadata pair. The primary use case for this endpoint is for doing large bulk runs of &gt;1000 files at a time that can process over the course of a few hours without needing to manage rate limits that would likely occur using the primary run endpoint.
     * <p>Unlike the single <a href="https://docs.extend.ai/2026-02-09/developers/api-reference/endpoints/workflow/run-workflow">Run Workflow</a> endpoint which returns the details of the created workflow runs immediately, this batch endpoint returns a <code>batchId</code>.</p>
     * <p>Our recommended usage pattern is to integrate with <a href="https://docs.extend.ai/2026-02-09/product/webhooks/configuration">Webhooks</a> for consuming results, using the <code>metadata</code> and <code>batchId</code> to match up results to the original inputs in your downstream systems. However, you can integrate in a polling mechanism by using a combination of the <a href="https://docs.extend.ai/2026-02-09/developers/api-reference/endpoints/workflow/list-workflow-runs">List Workflow Runs</a> endpoint to fetch all runs via a batch, and then <a href="https://docs.extend.ai/2026-02-09/developers/api-reference/endpoints/workflow/get-workflow-run">Get Workflow Run</a> to fetch the full outputs each run.</p>
     * <p><strong>Priority:</strong> All workflow runs created through this batch endpoint are automatically assigned a priority of 90.</p>
     * <p><strong>Processing and Monitoring:</strong>
     * Upon successful submission, the endpoint returns a <code>batchId</code>. The individual workflow runs are then queued for processing.</p>
     * <ul>
     * <li><strong>Monitoring:</strong> Track the progress and consume results of individual runs using <a href="https://docs.extend.ai/2026-02-09/product/webhooks/configuration">Webhooks</a>. Subscribe to events like <code>workflow_run.completed</code>, <code>workflow_run.failed</code>, etc. The webhook payload for these events will include the corresponding <code>batchId</code> and the <code>metadata</code> you provided for each input.</li>
     * <li><strong>Fetching Results:</strong> You can also use the <a href="https://docs.extend.ai/2026-02-09/developers/api-reference/endpoints/workflow/list-workflow-runs">List Workflow Runs</a> endpoint and filter using the <code>batchId</code> query param.</li>
     * </ul>
     */
    public CompletableFuture<WorkflowRunsCreateBatchResponse> createBatch(
            WorkflowRunsCreateBatchRequest request, RequestOptions requestOptions) {
        return this.rawClient.createBatch(request, requestOptions).thenApply(response -> response.body());
    }
}
