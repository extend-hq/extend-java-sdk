/**
 * This file was auto-generated by Fern from our API Definition.
 */
package ai.extend.resources.workflows;

import ai.extend.core.ClientOptions;
import ai.extend.core.RequestOptions;
import ai.extend.resources.workflows.requests.WorkflowsCreateRequest;
import ai.extend.resources.workflows.requests.WorkflowsDeployRequest;
import ai.extend.resources.workflows.requests.WorkflowsListRequest;
import ai.extend.resources.workflows.requests.WorkflowsListVersionsRequest;
import ai.extend.resources.workflows.requests.WorkflowsUpdateRequest;
import ai.extend.resources.workflows.types.WorkflowsListResponse;
import ai.extend.resources.workflows.types.WorkflowsListVersionsResponse;
import ai.extend.types.Workflow;
import ai.extend.types.WorkflowVersion;

public class WorkflowsClient {
    protected final ClientOptions clientOptions;

    private final RawWorkflowsClient rawClient;

    public WorkflowsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawWorkflowsClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawWorkflowsClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * List all workflows. Returns a paginated list of workflow summaries.
     */
    public WorkflowsListResponse list() {
        return this.rawClient.list().body();
    }

    /**
     * List all workflows. Returns a paginated list of workflow summaries.
     */
    public WorkflowsListResponse list(RequestOptions requestOptions) {
        return this.rawClient.list(requestOptions).body();
    }

    /**
     * List all workflows. Returns a paginated list of workflow summaries.
     */
    public WorkflowsListResponse list(WorkflowsListRequest request) {
        return this.rawClient.list(request).body();
    }

    /**
     * List all workflows. Returns a paginated list of workflow summaries.
     */
    public WorkflowsListResponse list(WorkflowsListRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(request, requestOptions).body();
    }

    /**
     * Create a new workflow. Optionally provide <code>steps</code> to define the workflow's step graph.
     * <p>When <code>steps</code> is omitted, the workflow is created with default steps (<code>TRIGGER</code> → <code>PARSE</code>). When <code>steps</code> is provided, the step graph is validated and the draft version is populated with the given steps.</p>
     * <p><strong>Note:</strong> The default steps may change in the future. If your integration depends on a specific step graph, provide <code>steps</code> explicitly.</p>
     */
    public Workflow create(WorkflowsCreateRequest request) {
        return this.rawClient.create(request).body();
    }

    /**
     * Create a new workflow. Optionally provide <code>steps</code> to define the workflow's step graph.
     * <p>When <code>steps</code> is omitted, the workflow is created with default steps (<code>TRIGGER</code> → <code>PARSE</code>). When <code>steps</code> is provided, the step graph is validated and the draft version is populated with the given steps.</p>
     * <p><strong>Note:</strong> The default steps may change in the future. If your integration depends on a specific step graph, provide <code>steps</code> explicitly.</p>
     */
    public Workflow create(WorkflowsCreateRequest request, RequestOptions requestOptions) {
        return this.rawClient.create(request, requestOptions).body();
    }

    /**
     * Get details of a workflow, including its draft version and steps.
     */
    public Workflow retrieve(String id) {
        return this.rawClient.retrieve(id).body();
    }

    /**
     * Get details of a workflow, including its draft version and steps.
     */
    public Workflow retrieve(String id, RequestOptions requestOptions) {
        return this.rawClient.retrieve(id, requestOptions).body();
    }

    /**
     * Update a workflow's draft. You can update the name, the steps, or both.
     * <p>When <code>steps</code> is provided, the draft version's steps are replaced with the new set. Steps with matching names from the previous draft preserve their internal identity.</p>
     */
    public Workflow update(String id) {
        return this.rawClient.update(id).body();
    }

    /**
     * Update a workflow's draft. You can update the name, the steps, or both.
     * <p>When <code>steps</code> is provided, the draft version's steps are replaced with the new set. Steps with matching names from the previous draft preserve their internal identity.</p>
     */
    public Workflow update(String id, RequestOptions requestOptions) {
        return this.rawClient.update(id, requestOptions).body();
    }

    /**
     * Update a workflow's draft. You can update the name, the steps, or both.
     * <p>When <code>steps</code> is provided, the draft version's steps are replaced with the new set. Steps with matching names from the previous draft preserve their internal identity.</p>
     */
    public Workflow update(String id, WorkflowsUpdateRequest request) {
        return this.rawClient.update(id, request).body();
    }

    /**
     * Update a workflow's draft. You can update the name, the steps, or both.
     * <p>When <code>steps</code> is provided, the draft version's steps are replaced with the new set. Steps with matching names from the previous draft preserve their internal identity.</p>
     */
    public Workflow update(String id, WorkflowsUpdateRequest request, RequestOptions requestOptions) {
        return this.rawClient.update(id, request, requestOptions).body();
    }

    /**
     * List all versions of a workflow, including the draft version. Returns a paginated list of version summaries.
     */
    public WorkflowsListVersionsResponse listVersions(String id) {
        return this.rawClient.listVersions(id).body();
    }

    /**
     * List all versions of a workflow, including the draft version. Returns a paginated list of version summaries.
     */
    public WorkflowsListVersionsResponse listVersions(String id, RequestOptions requestOptions) {
        return this.rawClient.listVersions(id, requestOptions).body();
    }

    /**
     * List all versions of a workflow, including the draft version. Returns a paginated list of version summaries.
     */
    public WorkflowsListVersionsResponse listVersions(String id, WorkflowsListVersionsRequest request) {
        return this.rawClient.listVersions(id, request).body();
    }

    /**
     * List all versions of a workflow, including the draft version. Returns a paginated list of version summaries.
     */
    public WorkflowsListVersionsResponse listVersions(
            String id, WorkflowsListVersionsRequest request, RequestOptions requestOptions) {
        return this.rawClient.listVersions(id, request, requestOptions).body();
    }

    /**
     * Deploy a new version of a workflow. The deployed version becomes available for running workflow runs.
     * <p>When <code>steps</code> is omitted, the current draft is deployed as-is. When <code>steps</code> is provided, the given steps are deployed directly without modifying the draft.</p>
     */
    public WorkflowVersion deploy(String id) {
        return this.rawClient.deploy(id).body();
    }

    /**
     * Deploy a new version of a workflow. The deployed version becomes available for running workflow runs.
     * <p>When <code>steps</code> is omitted, the current draft is deployed as-is. When <code>steps</code> is provided, the given steps are deployed directly without modifying the draft.</p>
     */
    public WorkflowVersion deploy(String id, RequestOptions requestOptions) {
        return this.rawClient.deploy(id, requestOptions).body();
    }

    /**
     * Deploy a new version of a workflow. The deployed version becomes available for running workflow runs.
     * <p>When <code>steps</code> is omitted, the current draft is deployed as-is. When <code>steps</code> is provided, the given steps are deployed directly without modifying the draft.</p>
     */
    public WorkflowVersion deploy(String id, WorkflowsDeployRequest request) {
        return this.rawClient.deploy(id, request).body();
    }

    /**
     * Deploy a new version of a workflow. The deployed version becomes available for running workflow runs.
     * <p>When <code>steps</code> is omitted, the current draft is deployed as-is. When <code>steps</code> is provided, the given steps are deployed directly without modifying the draft.</p>
     */
    public WorkflowVersion deploy(String id, WorkflowsDeployRequest request, RequestOptions requestOptions) {
        return this.rawClient.deploy(id, request, requestOptions).body();
    }

    /**
     * Get a specific version of a workflow, including its step definitions.
     * <p>The <code>versionId</code> parameter accepts:</p>
     * <ul>
     * <li><code>&quot;draft&quot;</code> — returns the current draft version</li>
     * <li>A version number (e.g. <code>&quot;1&quot;</code>, <code>&quot;2&quot;</code>) — returns that deployed version</li>
     * <li>An internal version ID (e.g. <code>&quot;workflow_version_abc123&quot;</code>) — returns that specific version</li>
     * </ul>
     */
    public WorkflowVersion retrieveVersion(String id, String versionId) {
        return this.rawClient.retrieveVersion(id, versionId).body();
    }

    /**
     * Get a specific version of a workflow, including its step definitions.
     * <p>The <code>versionId</code> parameter accepts:</p>
     * <ul>
     * <li><code>&quot;draft&quot;</code> — returns the current draft version</li>
     * <li>A version number (e.g. <code>&quot;1&quot;</code>, <code>&quot;2&quot;</code>) — returns that deployed version</li>
     * <li>An internal version ID (e.g. <code>&quot;workflow_version_abc123&quot;</code>) — returns that specific version</li>
     * </ul>
     */
    public WorkflowVersion retrieveVersion(String id, String versionId, RequestOptions requestOptions) {
        return this.rawClient.retrieveVersion(id, versionId, requestOptions).body();
    }
}
