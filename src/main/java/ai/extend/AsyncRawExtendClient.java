/**
 * Async raw-response Extend client - not auto-generated by Fern.
 *
 * <p>Fern generates {@link AsyncRawExtendClientBase}; this hand-written subclass preserves the
 * historical {@code AsyncRawExtendClient} type name (returned by {@link AsyncExtendClient#withRawResponse()}).</p>
 */
package ai.extend;

import ai.extend.core.ClientOptions;

/**
 * Provides access to raw HTTP responses (status code, headers) alongside the deserialized body,
 * with asynchronous (CompletableFuture) method signatures.
 */
public class AsyncRawExtendClient extends AsyncRawExtendClientBase {

    public AsyncRawExtendClient(ClientOptions clientOptions) {
        super(clientOptions);
    }
}
