/**
 * This file was auto-generated by Fern from our API Definition.
 */
package ai.extend.types;

import ai.extend.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = ExtractorRef.Builder.class)
public final class ExtractorRef {
    private final String id;

    private final String version;

    private final Map<String, Object> additionalProperties;

    private ExtractorRef(String id, String version, Map<String, Object> additionalProperties) {
        this.id = id;
        this.version = version;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The extractor ID.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return The extractor version to use: <code>&quot;latest&quot;</code> (most recent published version), <code>&quot;draft&quot;</code>, or a specific semver string (e.g. <code>&quot;1.0&quot;</code>).
     */
    @JsonProperty("version")
    public String getVersion() {
        return version;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof ExtractorRef && equalTo((ExtractorRef) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(ExtractorRef other) {
        return id.equals(other.id) && version.equals(other.version);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.id, this.version);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdStage builder() {
        return new Builder();
    }

    public interface IdStage {
        /**
         * <p>The extractor ID.</p>
         */
        VersionStage id(@NotNull String id);

        Builder from(ExtractorRef other);
    }

    public interface VersionStage {
        /**
         * <p>The extractor version to use: <code>&quot;latest&quot;</code> (most recent published version), <code>&quot;draft&quot;</code>, or a specific semver string (e.g. <code>&quot;1.0&quot;</code>).</p>
         */
        _FinalStage version(@NotNull String version);
    }

    public interface _FinalStage {
        ExtractorRef build();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements IdStage, VersionStage, _FinalStage {
        private String id;

        private String version;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(ExtractorRef other) {
            id(other.getId());
            version(other.getVersion());
            return this;
        }

        /**
         * <p>The extractor ID.</p>
         * <p>The extractor ID.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public VersionStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>The extractor version to use: <code>&quot;latest&quot;</code> (most recent published version), <code>&quot;draft&quot;</code>, or a specific semver string (e.g. <code>&quot;1.0&quot;</code>).</p>
         * <p>The extractor version to use: <code>&quot;latest&quot;</code> (most recent published version), <code>&quot;draft&quot;</code>, or a specific semver string (e.g. <code>&quot;1.0&quot;</code>).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("version")
        public _FinalStage version(@NotNull String version) {
            this.version = Objects.requireNonNull(version, "version must not be null");
            return this;
        }

        @java.lang.Override
        public ExtractorRef build() {
            return new ExtractorRef(id, version, additionalProperties);
        }
    }
}
