/**
 * This file was auto-generated by Fern from our API Definition.
 */
package ai.extend.types;

import ai.extend.core.Nullable;
import ai.extend.core.NullableNonemptyFilter;
import ai.extend.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = ConditionalExtractResult.Builder.class)
public final class ConditionalExtractResult {
    private final String ruleName;

    private final Optional<ExtractRun> extractRun;

    private final Map<String, Object> additionalProperties;

    private ConditionalExtractResult(
            String ruleName, Optional<ExtractRun> extractRun, Map<String, Object> additionalProperties) {
        this.ruleName = ruleName;
        this.extractRun = extractRun;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The name of the rule that was matched and used to route to the extractor.
     * <p>Example: <code>&quot;Invoice Type A&quot;</code></p>
     */
    @JsonProperty("ruleName")
    public String getRuleName() {
        return ruleName;
    }

    /**
     * @return The extract run that was executed based on the matched rule. Will be null if the conditional extract step was not successful.
     */
    @JsonIgnore
    public Optional<ExtractRun> getExtractRun() {
        if (extractRun == null) {
            return Optional.empty();
        }
        return extractRun;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("extractRun")
    private Optional<ExtractRun> _getExtractRun() {
        return extractRun;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof ConditionalExtractResult && equalTo((ConditionalExtractResult) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(ConditionalExtractResult other) {
        return ruleName.equals(other.ruleName) && extractRun.equals(other.extractRun);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.ruleName, this.extractRun);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static RuleNameStage builder() {
        return new Builder();
    }

    public interface RuleNameStage {
        /**
         * <p>The name of the rule that was matched and used to route to the extractor.</p>
         * <p>Example: <code>&quot;Invoice Type A&quot;</code></p>
         */
        _FinalStage ruleName(@NotNull String ruleName);

        Builder from(ConditionalExtractResult other);
    }

    public interface _FinalStage {
        ConditionalExtractResult build();

        /**
         * <p>The extract run that was executed based on the matched rule. Will be null if the conditional extract step was not successful.</p>
         */
        _FinalStage extractRun(Optional<ExtractRun> extractRun);

        _FinalStage extractRun(ExtractRun extractRun);

        _FinalStage extractRun(Nullable<ExtractRun> extractRun);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements RuleNameStage, _FinalStage {
        private String ruleName;

        private Optional<ExtractRun> extractRun = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(ConditionalExtractResult other) {
            ruleName(other.getRuleName());
            extractRun(other.getExtractRun());
            return this;
        }

        /**
         * <p>The name of the rule that was matched and used to route to the extractor.</p>
         * <p>Example: <code>&quot;Invoice Type A&quot;</code></p>
         * <p>The name of the rule that was matched and used to route to the extractor.</p>
         * <p>Example: <code>&quot;Invoice Type A&quot;</code></p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("ruleName")
        public _FinalStage ruleName(@NotNull String ruleName) {
            this.ruleName = Objects.requireNonNull(ruleName, "ruleName must not be null");
            return this;
        }

        /**
         * <p>The extract run that was executed based on the matched rule. Will be null if the conditional extract step was not successful.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage extractRun(Nullable<ExtractRun> extractRun) {
            if (extractRun.isNull()) {
                this.extractRun = null;
            } else if (extractRun.isEmpty()) {
                this.extractRun = Optional.empty();
            } else {
                this.extractRun = Optional.of(extractRun.get());
            }
            return this;
        }

        /**
         * <p>The extract run that was executed based on the matched rule. Will be null if the conditional extract step was not successful.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage extractRun(ExtractRun extractRun) {
            this.extractRun = Optional.ofNullable(extractRun);
            return this;
        }

        /**
         * <p>The extract run that was executed based on the matched rule. Will be null if the conditional extract step was not successful.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "extractRun", nulls = Nulls.SKIP)
        public _FinalStage extractRun(Optional<ExtractRun> extractRun) {
            this.extractRun = extractRun;
            return this;
        }

        @java.lang.Override
        public ConditionalExtractResult build() {
            return new ConditionalExtractResult(ruleName, extractRun, additionalProperties);
        }
    }
}
