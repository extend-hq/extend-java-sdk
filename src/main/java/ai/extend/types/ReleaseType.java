/**
 * This file was auto-generated by Fern from our API Definition.
 */
package ai.extend.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class ReleaseType {
    public static final ReleaseType MINOR = new ReleaseType(Value.MINOR, "minor");

    public static final ReleaseType MAJOR = new ReleaseType(Value.MAJOR, "major");

    private final Value value;

    private final String string;

    ReleaseType(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other) || (other instanceof ReleaseType && this.string.equals(((ReleaseType) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case MINOR:
                return visitor.visitMinor();
            case MAJOR:
                return visitor.visitMajor();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static ReleaseType valueOf(String value) {
        switch (value) {
            case "minor":
                return MINOR;
            case "major":
                return MAJOR;
            default:
                return new ReleaseType(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        MAJOR,

        MINOR,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitMajor();

        T visitMinor();

        T visitUnknown(String unknownType);
    }
}
