/**
 * This file was auto-generated by Fern from our API Definition.
 */
package ai.extend.types;

import ai.extend.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = WebhookPayloadOptions.Builder.class)
public final class WebhookPayloadOptions {
    private final WebhookPayloadFormat format;

    private final Optional<Integer> urlThresholdBytes;

    private final Map<String, Object> additionalProperties;

    private WebhookPayloadOptions(
            WebhookPayloadFormat format,
            Optional<Integer> urlThresholdBytes,
            Map<String, Object> additionalProperties) {
        this.format = format;
        this.urlThresholdBytes = urlThresholdBytes;
        this.additionalProperties = additionalProperties;
    }

    @JsonProperty("format")
    public WebhookPayloadFormat getFormat() {
        return format;
    }

    /**
     * @return When <code>format</code> is <code>url</code>, payloads larger than this threshold (in bytes) are delivered as a signed download URL instead of inline JSON. Only applicable when <code>format</code> is <code>url</code>.
     */
    @JsonProperty("urlThresholdBytes")
    public Optional<Integer> getUrlThresholdBytes() {
        return urlThresholdBytes;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof WebhookPayloadOptions && equalTo((WebhookPayloadOptions) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(WebhookPayloadOptions other) {
        return format.equals(other.format) && urlThresholdBytes.equals(other.urlThresholdBytes);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.format, this.urlThresholdBytes);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static FormatStage builder() {
        return new Builder();
    }

    public interface FormatStage {
        _FinalStage format(@NotNull WebhookPayloadFormat format);

        Builder from(WebhookPayloadOptions other);
    }

    public interface _FinalStage {
        WebhookPayloadOptions build();

        /**
         * <p>When <code>format</code> is <code>url</code>, payloads larger than this threshold (in bytes) are delivered as a signed download URL instead of inline JSON. Only applicable when <code>format</code> is <code>url</code>.</p>
         */
        _FinalStage urlThresholdBytes(Optional<Integer> urlThresholdBytes);

        _FinalStage urlThresholdBytes(Integer urlThresholdBytes);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements FormatStage, _FinalStage {
        private WebhookPayloadFormat format;

        private Optional<Integer> urlThresholdBytes = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(WebhookPayloadOptions other) {
            format(other.getFormat());
            urlThresholdBytes(other.getUrlThresholdBytes());
            return this;
        }

        @java.lang.Override
        @JsonSetter("format")
        public _FinalStage format(@NotNull WebhookPayloadFormat format) {
            this.format = Objects.requireNonNull(format, "format must not be null");
            return this;
        }

        /**
         * <p>When <code>format</code> is <code>url</code>, payloads larger than this threshold (in bytes) are delivered as a signed download URL instead of inline JSON. Only applicable when <code>format</code> is <code>url</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage urlThresholdBytes(Integer urlThresholdBytes) {
            this.urlThresholdBytes = Optional.ofNullable(urlThresholdBytes);
            return this;
        }

        /**
         * <p>When <code>format</code> is <code>url</code>, payloads larger than this threshold (in bytes) are delivered as a signed download URL instead of inline JSON. Only applicable when <code>format</code> is <code>url</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "urlThresholdBytes", nulls = Nulls.SKIP)
        public _FinalStage urlThresholdBytes(Optional<Integer> urlThresholdBytes) {
            this.urlThresholdBytes = urlThresholdBytes;
            return this;
        }

        @java.lang.Override
        public WebhookPayloadOptions build() {
            return new WebhookPayloadOptions(format, urlThresholdBytes, additionalProperties);
        }
    }
}
