/**
 * This file was auto-generated by Fern from our API Definition.
 */
package ai.extend.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class WorkflowStepDefinition {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private WorkflowStepDefinition(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static WorkflowStepDefinition trigger(TriggerStepDefinition value) {
        return new WorkflowStepDefinition(new TriggerValue(value));
    }

    public static WorkflowStepDefinition parse(ParseStepDefinition value) {
        return new WorkflowStepDefinition(new ParseValue(value));
    }

    public static WorkflowStepDefinition extract(ExtractStepDefinition value) {
        return new WorkflowStepDefinition(new ExtractValue(value));
    }

    public static WorkflowStepDefinition classify(ClassifyStepDefinition value) {
        return new WorkflowStepDefinition(new ClassifyValue(value));
    }

    public static WorkflowStepDefinition split(SplitStepDefinition value) {
        return new WorkflowStepDefinition(new SplitValue(value));
    }

    public static WorkflowStepDefinition mergeExtract(MergeExtractStepDefinition value) {
        return new WorkflowStepDefinition(new MergeExtractValue(value));
    }

    public static WorkflowStepDefinition conditional(ConditionalStepDefinition value) {
        return new WorkflowStepDefinition(new ConditionalValue(value));
    }

    public static WorkflowStepDefinition conditionalExtract(ConditionalExtractStepDefinition value) {
        return new WorkflowStepDefinition(new ConditionalExtractValue(value));
    }

    public static WorkflowStepDefinition externalDataValidation(ExternalDataValidationStepDefinition value) {
        return new WorkflowStepDefinition(new ExternalDataValidationValue(value));
    }

    public static WorkflowStepDefinition webhookResponse(WebhookResponseStepDefinition value) {
        return new WorkflowStepDefinition(new WebhookResponseValue(value));
    }

    public static WorkflowStepDefinition ruleValidation(RuleValidationStepDefinition value) {
        return new WorkflowStepDefinition(new RuleValidationValue(value));
    }

    public static WorkflowStepDefinition humanReview(HumanReviewStepDefinition value) {
        return new WorkflowStepDefinition(new HumanReviewValue(value));
    }

    public static WorkflowStepDefinition collect(CollectStepDefinition value) {
        return new WorkflowStepDefinition(new CollectValue(value));
    }

    public static WorkflowStepDefinition fileConversion(FileConversionStepDefinition value) {
        return new WorkflowStepDefinition(new FileConversionValue(value));
    }

    public boolean isTrigger() {
        return value instanceof TriggerValue;
    }

    public boolean isParse() {
        return value instanceof ParseValue;
    }

    public boolean isExtract() {
        return value instanceof ExtractValue;
    }

    public boolean isClassify() {
        return value instanceof ClassifyValue;
    }

    public boolean isSplit() {
        return value instanceof SplitValue;
    }

    public boolean isMergeExtract() {
        return value instanceof MergeExtractValue;
    }

    public boolean isConditional() {
        return value instanceof ConditionalValue;
    }

    public boolean isConditionalExtract() {
        return value instanceof ConditionalExtractValue;
    }

    public boolean isExternalDataValidation() {
        return value instanceof ExternalDataValidationValue;
    }

    public boolean isWebhookResponse() {
        return value instanceof WebhookResponseValue;
    }

    public boolean isRuleValidation() {
        return value instanceof RuleValidationValue;
    }

    public boolean isHumanReview() {
        return value instanceof HumanReviewValue;
    }

    public boolean isCollect() {
        return value instanceof CollectValue;
    }

    public boolean isFileConversion() {
        return value instanceof FileConversionValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<TriggerStepDefinition> getTrigger() {
        if (isTrigger()) {
            return Optional.of(((TriggerValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ParseStepDefinition> getParse() {
        if (isParse()) {
            return Optional.of(((ParseValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ExtractStepDefinition> getExtract() {
        if (isExtract()) {
            return Optional.of(((ExtractValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ClassifyStepDefinition> getClassify() {
        if (isClassify()) {
            return Optional.of(((ClassifyValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<SplitStepDefinition> getSplit() {
        if (isSplit()) {
            return Optional.of(((SplitValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<MergeExtractStepDefinition> getMergeExtract() {
        if (isMergeExtract()) {
            return Optional.of(((MergeExtractValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ConditionalStepDefinition> getConditional() {
        if (isConditional()) {
            return Optional.of(((ConditionalValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ConditionalExtractStepDefinition> getConditionalExtract() {
        if (isConditionalExtract()) {
            return Optional.of(((ConditionalExtractValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ExternalDataValidationStepDefinition> getExternalDataValidation() {
        if (isExternalDataValidation()) {
            return Optional.of(((ExternalDataValidationValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<WebhookResponseStepDefinition> getWebhookResponse() {
        if (isWebhookResponse()) {
            return Optional.of(((WebhookResponseValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<RuleValidationStepDefinition> getRuleValidation() {
        if (isRuleValidation()) {
            return Optional.of(((RuleValidationValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<HumanReviewStepDefinition> getHumanReview() {
        if (isHumanReview()) {
            return Optional.of(((HumanReviewValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<CollectStepDefinition> getCollect() {
        if (isCollect()) {
            return Optional.of(((CollectValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<FileConversionStepDefinition> getFileConversion() {
        if (isFileConversion()) {
            return Optional.of(((FileConversionValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitTrigger(TriggerStepDefinition trigger);

        T visitParse(ParseStepDefinition parse);

        T visitExtract(ExtractStepDefinition extract);

        T visitClassify(ClassifyStepDefinition classify);

        T visitSplit(SplitStepDefinition split);

        T visitMergeExtract(MergeExtractStepDefinition mergeExtract);

        T visitConditional(ConditionalStepDefinition conditional);

        T visitConditionalExtract(ConditionalExtractStepDefinition conditionalExtract);

        T visitExternalDataValidation(ExternalDataValidationStepDefinition externalDataValidation);

        T visitWebhookResponse(WebhookResponseStepDefinition webhookResponse);

        T visitRuleValidation(RuleValidationStepDefinition ruleValidation);

        T visitHumanReview(HumanReviewStepDefinition humanReview);

        T visitCollect(CollectStepDefinition collect);

        T visitFileConversion(FileConversionStepDefinition fileConversion);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(TriggerValue.class),
        @JsonSubTypes.Type(ParseValue.class),
        @JsonSubTypes.Type(ExtractValue.class),
        @JsonSubTypes.Type(ClassifyValue.class),
        @JsonSubTypes.Type(SplitValue.class),
        @JsonSubTypes.Type(MergeExtractValue.class),
        @JsonSubTypes.Type(ConditionalValue.class),
        @JsonSubTypes.Type(ConditionalExtractValue.class),
        @JsonSubTypes.Type(ExternalDataValidationValue.class),
        @JsonSubTypes.Type(WebhookResponseValue.class),
        @JsonSubTypes.Type(RuleValidationValue.class),
        @JsonSubTypes.Type(HumanReviewValue.class),
        @JsonSubTypes.Type(CollectValue.class),
        @JsonSubTypes.Type(FileConversionValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("TRIGGER")
    @JsonIgnoreProperties("type")
    private static final class TriggerValue implements Value {
        @JsonUnwrapped
        private TriggerStepDefinition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private TriggerValue() {}

        private TriggerValue(TriggerStepDefinition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitTrigger(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof TriggerValue && equalTo((TriggerValue) other);
        }

        private boolean equalTo(TriggerValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "WorkflowStepDefinition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("PARSE")
    @JsonIgnoreProperties("type")
    private static final class ParseValue implements Value {
        @JsonUnwrapped
        private ParseStepDefinition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ParseValue() {}

        private ParseValue(ParseStepDefinition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitParse(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ParseValue && equalTo((ParseValue) other);
        }

        private boolean equalTo(ParseValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "WorkflowStepDefinition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("EXTRACT")
    @JsonIgnoreProperties("type")
    private static final class ExtractValue implements Value {
        @JsonUnwrapped
        private ExtractStepDefinition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ExtractValue() {}

        private ExtractValue(ExtractStepDefinition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitExtract(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ExtractValue && equalTo((ExtractValue) other);
        }

        private boolean equalTo(ExtractValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "WorkflowStepDefinition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("CLASSIFY")
    @JsonIgnoreProperties("type")
    private static final class ClassifyValue implements Value {
        @JsonUnwrapped
        private ClassifyStepDefinition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ClassifyValue() {}

        private ClassifyValue(ClassifyStepDefinition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitClassify(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ClassifyValue && equalTo((ClassifyValue) other);
        }

        private boolean equalTo(ClassifyValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "WorkflowStepDefinition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("SPLIT")
    @JsonIgnoreProperties("type")
    private static final class SplitValue implements Value {
        @JsonUnwrapped
        private SplitStepDefinition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private SplitValue() {}

        private SplitValue(SplitStepDefinition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitSplit(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof SplitValue && equalTo((SplitValue) other);
        }

        private boolean equalTo(SplitValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "WorkflowStepDefinition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("MERGE_EXTRACT")
    @JsonIgnoreProperties("type")
    private static final class MergeExtractValue implements Value {
        @JsonUnwrapped
        private MergeExtractStepDefinition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private MergeExtractValue() {}

        private MergeExtractValue(MergeExtractStepDefinition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitMergeExtract(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof MergeExtractValue && equalTo((MergeExtractValue) other);
        }

        private boolean equalTo(MergeExtractValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "WorkflowStepDefinition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("CONDITIONAL")
    @JsonIgnoreProperties("type")
    private static final class ConditionalValue implements Value {
        @JsonUnwrapped
        private ConditionalStepDefinition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ConditionalValue() {}

        private ConditionalValue(ConditionalStepDefinition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitConditional(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ConditionalValue && equalTo((ConditionalValue) other);
        }

        private boolean equalTo(ConditionalValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "WorkflowStepDefinition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("CONDITIONAL_EXTRACT")
    @JsonIgnoreProperties("type")
    private static final class ConditionalExtractValue implements Value {
        @JsonUnwrapped
        private ConditionalExtractStepDefinition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ConditionalExtractValue() {}

        private ConditionalExtractValue(ConditionalExtractStepDefinition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitConditionalExtract(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ConditionalExtractValue && equalTo((ConditionalExtractValue) other);
        }

        private boolean equalTo(ConditionalExtractValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "WorkflowStepDefinition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("EXTERNAL_DATA_VALIDATION")
    @JsonIgnoreProperties("type")
    private static final class ExternalDataValidationValue implements Value {
        @JsonUnwrapped
        private ExternalDataValidationStepDefinition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ExternalDataValidationValue() {}

        private ExternalDataValidationValue(ExternalDataValidationStepDefinition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitExternalDataValidation(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ExternalDataValidationValue && equalTo((ExternalDataValidationValue) other);
        }

        private boolean equalTo(ExternalDataValidationValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "WorkflowStepDefinition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("WEBHOOK_RESPONSE")
    @JsonIgnoreProperties("type")
    private static final class WebhookResponseValue implements Value {
        @JsonUnwrapped
        private WebhookResponseStepDefinition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private WebhookResponseValue() {}

        private WebhookResponseValue(WebhookResponseStepDefinition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitWebhookResponse(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof WebhookResponseValue && equalTo((WebhookResponseValue) other);
        }

        private boolean equalTo(WebhookResponseValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "WorkflowStepDefinition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("RULE_VALIDATION")
    @JsonIgnoreProperties("type")
    private static final class RuleValidationValue implements Value {
        @JsonUnwrapped
        private RuleValidationStepDefinition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private RuleValidationValue() {}

        private RuleValidationValue(RuleValidationStepDefinition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitRuleValidation(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof RuleValidationValue && equalTo((RuleValidationValue) other);
        }

        private boolean equalTo(RuleValidationValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "WorkflowStepDefinition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("HUMAN_REVIEW")
    @JsonIgnoreProperties("type")
    private static final class HumanReviewValue implements Value {
        @JsonUnwrapped
        private HumanReviewStepDefinition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private HumanReviewValue() {}

        private HumanReviewValue(HumanReviewStepDefinition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitHumanReview(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof HumanReviewValue && equalTo((HumanReviewValue) other);
        }

        private boolean equalTo(HumanReviewValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "WorkflowStepDefinition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("COLLECT")
    @JsonIgnoreProperties("type")
    private static final class CollectValue implements Value {
        @JsonUnwrapped
        private CollectStepDefinition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CollectValue() {}

        private CollectValue(CollectStepDefinition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitCollect(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CollectValue && equalTo((CollectValue) other);
        }

        private boolean equalTo(CollectValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "WorkflowStepDefinition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("FILE_CONVERSION")
    @JsonIgnoreProperties("type")
    private static final class FileConversionValue implements Value {
        @JsonUnwrapped
        private FileConversionStepDefinition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private FileConversionValue() {}

        private FileConversionValue(FileConversionStepDefinition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitFileConversion(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof FileConversionValue && equalTo((FileConversionValue) other);
        }

        private boolean equalTo(FileConversionValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "WorkflowStepDefinition{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "WorkflowStepDefinition{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
