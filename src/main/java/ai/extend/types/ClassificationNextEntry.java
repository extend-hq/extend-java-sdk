/**
 * This file was auto-generated by Fern from our API Definition.
 */
package ai.extend.types;

import ai.extend.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = ClassificationNextEntry.Builder.class)
public final class ClassificationNextEntry {
    private final String step;

    private final String classificationId;

    private final Map<String, Object> additionalProperties;

    private ClassificationNextEntry(String step, String classificationId, Map<String, Object> additionalProperties) {
        this.step = step;
        this.classificationId = classificationId;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The name of the target step.
     */
    @JsonProperty("step")
    public String getStep() {
        return step;
    }

    /**
     * @return Must match a classification <code>id</code> from the referenced classifier's or splitter's configuration (e.g. <code>&quot;cls_invoice&quot;</code>). Classification IDs are stable across renames — if a classification type is renamed, the ID stays the same and routing continues to work.
     */
    @JsonProperty("classificationId")
    public String getClassificationId() {
        return classificationId;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof ClassificationNextEntry && equalTo((ClassificationNextEntry) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(ClassificationNextEntry other) {
        return step.equals(other.step) && classificationId.equals(other.classificationId);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.step, this.classificationId);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static StepStage builder() {
        return new Builder();
    }

    public interface StepStage {
        /**
         * <p>The name of the target step.</p>
         */
        ClassificationIdStage step(@NotNull String step);

        Builder from(ClassificationNextEntry other);
    }

    public interface ClassificationIdStage {
        /**
         * <p>Must match a classification <code>id</code> from the referenced classifier's or splitter's configuration (e.g. <code>&quot;cls_invoice&quot;</code>). Classification IDs are stable across renames — if a classification type is renamed, the ID stays the same and routing continues to work.</p>
         */
        _FinalStage classificationId(@NotNull String classificationId);
    }

    public interface _FinalStage {
        ClassificationNextEntry build();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements StepStage, ClassificationIdStage, _FinalStage {
        private String step;

        private String classificationId;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(ClassificationNextEntry other) {
            step(other.getStep());
            classificationId(other.getClassificationId());
            return this;
        }

        /**
         * <p>The name of the target step.</p>
         * <p>The name of the target step.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("step")
        public ClassificationIdStage step(@NotNull String step) {
            this.step = Objects.requireNonNull(step, "step must not be null");
            return this;
        }

        /**
         * <p>Must match a classification <code>id</code> from the referenced classifier's or splitter's configuration (e.g. <code>&quot;cls_invoice&quot;</code>). Classification IDs are stable across renames — if a classification type is renamed, the ID stays the same and routing continues to work.</p>
         * <p>Must match a classification <code>id</code> from the referenced classifier's or splitter's configuration (e.g. <code>&quot;cls_invoice&quot;</code>). Classification IDs are stable across renames — if a classification type is renamed, the ID stays the same and routing continues to work.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("classificationId")
        public _FinalStage classificationId(@NotNull String classificationId) {
            this.classificationId = Objects.requireNonNull(classificationId, "classificationId must not be null");
            return this;
        }

        @java.lang.Override
        public ClassificationNextEntry build() {
            return new ClassificationNextEntry(step, classificationId, additionalProperties);
        }
    }
}
