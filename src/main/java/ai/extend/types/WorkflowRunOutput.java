/**
 * This file was auto-generated by Fern from our API Definition.
 */
package ai.extend.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class WorkflowRunOutput {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private WorkflowRunOutput(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static WorkflowRunOutput extractRun(ExtractRun value) {
        return new WorkflowRunOutput(new ExtractRunValue(value));
    }

    public static WorkflowRunOutput classifyRun(ClassifyRun value) {
        return new WorkflowRunOutput(new ClassifyRunValue(value));
    }

    public static WorkflowRunOutput splitRun(SplitRun value) {
        return new WorkflowRunOutput(new SplitRunValue(value));
    }

    public boolean isExtractRun() {
        return value instanceof ExtractRunValue;
    }

    public boolean isClassifyRun() {
        return value instanceof ClassifyRunValue;
    }

    public boolean isSplitRun() {
        return value instanceof SplitRunValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<ExtractRun> getExtractRun() {
        if (isExtractRun()) {
            return Optional.of(((ExtractRunValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ClassifyRun> getClassifyRun() {
        if (isClassifyRun()) {
            return Optional.of(((ClassifyRunValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<SplitRun> getSplitRun() {
        if (isSplitRun()) {
            return Optional.of(((SplitRunValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitExtractRun(ExtractRun extractRun);

        T visitClassifyRun(ClassifyRun classifyRun);

        T visitSplitRun(SplitRun splitRun);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "object", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(ExtractRunValue.class),
        @JsonSubTypes.Type(ClassifyRunValue.class),
        @JsonSubTypes.Type(SplitRunValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("extract_run")
    @JsonIgnoreProperties("object")
    private static final class ExtractRunValue implements Value {
        @JsonUnwrapped
        private ExtractRun value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ExtractRunValue() {}

        private ExtractRunValue(ExtractRun value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitExtractRun(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ExtractRunValue && equalTo((ExtractRunValue) other);
        }

        private boolean equalTo(ExtractRunValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "WorkflowRunOutput{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("classify_run")
    @JsonIgnoreProperties("object")
    private static final class ClassifyRunValue implements Value {
        @JsonUnwrapped
        private ClassifyRun value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ClassifyRunValue() {}

        private ClassifyRunValue(ClassifyRun value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitClassifyRun(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ClassifyRunValue && equalTo((ClassifyRunValue) other);
        }

        private boolean equalTo(ClassifyRunValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "WorkflowRunOutput{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("split_run")
    @JsonIgnoreProperties("object")
    private static final class SplitRunValue implements Value {
        @JsonUnwrapped
        private SplitRun value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private SplitRunValue() {}

        private SplitRunValue(SplitRun value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitSplitRun(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof SplitRunValue && equalTo((SplitRunValue) other);
        }

        private boolean equalTo(SplitRunValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "WorkflowRunOutput{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("object")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "WorkflowRunOutput{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
