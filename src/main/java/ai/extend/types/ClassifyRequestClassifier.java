/**
 * This file was auto-generated by Fern from our API Definition.
 */
package ai.extend.types;

import ai.extend.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = ClassifyRequestClassifier.Builder.class)
public final class ClassifyRequestClassifier {
    private final String id;

    private final Optional<String> version;

    private final Optional<ClassifyOverrideConfig> overrideConfig;

    private final Map<String, Object> additionalProperties;

    private ClassifyRequestClassifier(
            String id,
            Optional<String> version,
            Optional<ClassifyOverrideConfig> overrideConfig,
            Map<String, Object> additionalProperties) {
        this.id = id;
        this.version = version;
        this.overrideConfig = overrideConfig;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The ID of the classifier to use.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    @JsonProperty("version")
    public Optional<String> getVersion() {
        return version;
    }

    /**
     * @return Optional partial configuration override. Only the fields you provide will override the classifier's saved configuration. For example, you can pass only <code>classificationRules</code> without providing <code>classifications</code>.
     */
    @JsonProperty("overrideConfig")
    public Optional<ClassifyOverrideConfig> getOverrideConfig() {
        return overrideConfig;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof ClassifyRequestClassifier && equalTo((ClassifyRequestClassifier) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(ClassifyRequestClassifier other) {
        return id.equals(other.id) && version.equals(other.version) && overrideConfig.equals(other.overrideConfig);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.id, this.version, this.overrideConfig);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdStage builder() {
        return new Builder();
    }

    public interface IdStage {
        /**
         * <p>The ID of the classifier to use.</p>
         */
        _FinalStage id(@NotNull String id);

        Builder from(ClassifyRequestClassifier other);
    }

    public interface _FinalStage {
        ClassifyRequestClassifier build();

        _FinalStage version(Optional<String> version);

        _FinalStage version(String version);

        /**
         * <p>Optional partial configuration override. Only the fields you provide will override the classifier's saved configuration. For example, you can pass only <code>classificationRules</code> without providing <code>classifications</code>.</p>
         */
        _FinalStage overrideConfig(Optional<ClassifyOverrideConfig> overrideConfig);

        _FinalStage overrideConfig(ClassifyOverrideConfig overrideConfig);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements IdStage, _FinalStage {
        private String id;

        private Optional<ClassifyOverrideConfig> overrideConfig = Optional.empty();

        private Optional<String> version = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(ClassifyRequestClassifier other) {
            id(other.getId());
            version(other.getVersion());
            overrideConfig(other.getOverrideConfig());
            return this;
        }

        /**
         * <p>The ID of the classifier to use.</p>
         * <p>The ID of the classifier to use.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public _FinalStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>Optional partial configuration override. Only the fields you provide will override the classifier's saved configuration. For example, you can pass only <code>classificationRules</code> without providing <code>classifications</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage overrideConfig(ClassifyOverrideConfig overrideConfig) {
            this.overrideConfig = Optional.ofNullable(overrideConfig);
            return this;
        }

        /**
         * <p>Optional partial configuration override. Only the fields you provide will override the classifier's saved configuration. For example, you can pass only <code>classificationRules</code> without providing <code>classifications</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "overrideConfig", nulls = Nulls.SKIP)
        public _FinalStage overrideConfig(Optional<ClassifyOverrideConfig> overrideConfig) {
            this.overrideConfig = overrideConfig;
            return this;
        }

        @java.lang.Override
        public _FinalStage version(String version) {
            this.version = Optional.ofNullable(version);
            return this;
        }

        @java.lang.Override
        @JsonSetter(value = "version", nulls = Nulls.SKIP)
        public _FinalStage version(Optional<String> version) {
            this.version = version;
            return this;
        }

        @java.lang.Override
        public ClassifyRequestClassifier build() {
            return new ClassifyRequestClassifier(id, version, overrideConfig, additionalProperties);
        }
    }
}
